"""Optional integrations for hierarchical classification.

Each integration requires its own optional dependency group.

Install with:
    uv pip install hierarchical-classification[sklearn]
    uv pip install hierarchical-classification[huggingface]
    uv pip install hierarchical-classification[llm]
"""
