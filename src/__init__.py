"""Base module for hierarchical classification.

Optional integrations are available in :mod:`src.integrations`:

- :mod:`src.integrations.sklearn` — wrap any sklearn-compatible classifier
  (install with ``pip install hierarchical-classification[sklearn]``)
- :mod:`src.integrations.huggingface` — HuggingFace zero-shot classification
  (install with ``pip install hierarchical-classification[huggingface]``)
- :mod:`src.integrations.llm` — LLM-backed classification via litellm
  (install with ``pip install hierarchical-classification[llm]``)
"""
